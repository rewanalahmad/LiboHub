[pytest]
DJANGO_SETTINGS_MODULE = localcart.settings

# Accept all these file patterns
python_files = 
    test.py 
    test_*.py 
    *_test.py

# Reuse DB and skip migrations
addopts =
    --reuse-db
    --nomigrations

markers =
    slow: slow running test
